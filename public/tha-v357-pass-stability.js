(() => {
  const ID='tha-v357-pass-stability';
  if(window[ID])return;window[ID]=true;
  function nextYear(){const date=new Date();date.setFullYear(date.getFullYear()+1);return date.toLocaleDateString([],{year:'numeric',month:'short',day:'numeric'})}
  function styles(){if(document.getElementById(`${ID}-styles`))return;const style=document.createElement('style');style.id=`${ID}-styles`;style.textContent=`.passReviewGrid{align-items:start}.passReviewCard.pmcp-declined{border-color:#cbb1da!important;box-shadow:inset 6px 0 0 #7e4c9a!important;background:#fcf8fe!important}.passReviewCard.pmcp-declined .passWorkflowBadge{background:#f0e6f6!important;border-color:#d9c5e5!important;color:#5c3774!important}.passReviewCard.pmcp-declined .secondaryBtn.thaNotThisYearActive{background:#7e4c9a!important;border-color:#7e4c9a!important;color:#fff!important}.thaDeferredReminder{display:inline-flex;margin:7px 0 0;border-radius:999px;background:#f0e6f6;border:1px solid #d9c5e5;color:#5c3774;padding:5px 9px;font-size:11px;font-weight:900}`;document.head.append(style)}
  function stableOrder(){document.querySelectorAll('.passReviewGrid').forEach(grid=>{const cards=Array.from(grid.querySelectorAll(':scope > .passReviewCard'));const titles=cards.map(card=>(card.querySelector('h4')?.textContent||'').trim()).sort((a,b)=>a.localeCompare(b));const order=new Map(titles.map((title,index)=>[title,index]));cards.forEach(card=>{card.style.order=String(order.get((card.querySelector('h4')?.textContent||'').trim())??999)})})}
  function decorate(){document.querySelectorAll('.passReviewCard').forEach(card=>{const declined=card.classList.contains('pmcp-declined');const button=Array.from(card.querySelectorAll('button')).find(item=>/^Not this year/i.test(item.textContent||''));if(button){button.classList.toggle('thaNotThisYearActive',declined);button.textContent=declined?'Not this year ✓':'Not this year'}const badge=card.querySelector('.passWorkflowBadge');if(badge&&declined)badge.textContent='Deferred · future reminder retained';let reminder=card.querySelector('.thaDeferredReminder');if(declined&&!reminder){reminder=document.createElement('span');reminder.className='thaDeferredReminder';card.querySelector('.passReviewTitle')?.append(reminder)}if(reminder){reminder.textContent=`Not this year · review again by ${nextYear()}`;reminder.hidden=!declined}})}
  function run(){styles();stableOrder();decorate()}
  let pending=false;function schedule(){if(pending)return;pending=true;requestAnimationFrame(()=>{pending=false;run()})}schedule();setTimeout(schedule,500);new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});
})();
